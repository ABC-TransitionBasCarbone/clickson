export interface School {
    id?: string,
    state?: string,
    name?: string,
    townName?: string,
    postalCode?: string,
    studentCount?: number,
    staffCount?: number,
    establishmentYear?: number,
    adress?: string
}
