export class Category {
    id: string;
    label: string;
    details: string;

    constructor(id: string, label: string, details: string) {
        this.id = id;
        this.label = label;
        this.details = details;
    }
}
